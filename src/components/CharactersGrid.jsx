
export const CharactersGrid = ({status}) => {

    //here is where i am going to use the custom hook to get the characters
    
    return (
    <div>
        <h1>{status}</h1>
        {

        }
        <div className="card-grid">
            {
                
            }
        </div>
    </div>
  )
}
